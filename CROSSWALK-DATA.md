# 서울 횡단보도 위치 후보

## 관측 시각 연결 확인 (2026-09-18)

`SignalTimingConnection`은 교차로 이름·80m 위치·유일 방향 후보를 통해 저장된 T-Data 관측값을 연결한다. 관측 시각+1/10초 단위 잔여시간을 해당 상태의 전환 예정 시각으로 표시한다. 미래 원본 시각, 30초 초과 관측, 이미 끝난 상태, 같은 시각의 충돌 응답, 다른 교차로 ID, 미해석 상태를 제외한다. 누적 예상 도착이 fresh `protected-Movement-Allowed` 관측 구간 안에 있고 끝에서 2초 이상 떨어져 있을 때만 0초를 사용한다. 좌표 대응과 실제 횡단은 여전히 추정이며, 실시간 통행 안내가 아니다. 그 밖은 기존 평균·기본 주기 모델을 유지한다. 관측 잔여시간을 한 주기 전체로 반복하지 않는다.

경찰청 교통신호기 관리 업무편람의 교통신호제어기 표준규격은 옵셋 적용 시점을 자정, 연동 기준을 지정 주현시의 시작으로 설명한다. 출처: https://itskorea.kr/downloadFile.do?fileId=FILE_000000000079174&fileSn=2 (검색 색인 본문 확인). 이것은 자정+옵셋이 해당 보행신호의 녹색 시작이라는 뜻이 아니다. 현재 API 운영계획은 주기·옵셋·A/B링 현시를, 기반정보는 맵별 방향코드를 제공한다. 적용 맵·주현시·보행 진입/점멸 구간이 확정되지 않아 여러 주기 뒤의 정확한 예측은 활성화하지 않았다. 공식 UTIC 설명 https://www.utic.go.kr/guide/utisRefSig.do 에서는 시그널맵을 별도 자료로 구분한다. 이번 테스트는 합성 관측값으로 시각·취소가 아닌 경계·자료 충돌을 검사했으며 인증된 최신 현장 응답과의 실증을 수행한 것은 아니다.

## 2026-09-18 주기 기반 추정 모드

후속 사용자 요청으로 누락 주기의 처리 변경: 신호등 미설치는 계산 대상에서 제외하며, 개별 주기가 없으면 수집된 교차로별 주기 평균들의 평균을 대입한다. 유효 주기가 전혀 없으면 기본 120초를 가정한다(측정 평균이 아님). 설치 여부 미확인은 있음으로 가정한다. 빈 근접 자료는 검출된 시설에 대한 0초로 집계하며 누락 가능성을 표시한다. 단일 후보는 비교 단축 0초, 더 느린 후보를 수동 선택하면 단축 0초이다. 아래 누락 시 계산 보류 설명은 이 추정 모드에는 더 이상 적용되지 않으며, 실제 보행신호 예측 모드의 검증 조건만 유지된다.

사용자 요청에 따라 실제 보행현시 예측과 별개인 `cycleHeuristic`을 사용한다. 빨간불 비율 50%, 무작위 도착, 근접 횡단보도를 실제 건넘으로 가정해 주기 / 8초를 각 방문에 합산한다. 수집된 운영계획의 이름이 일치하고 경찰청 교차로 ID가 유일할 때만 연결한다. 적용 계획 후보를 정할 수 없으면 해당 교차로의 수집된 운영주기를 동일 가중 평균하며, 이는 현재 제어기의 실제 주기라는 뜻이 아니다. 설치 여부 미확인, 주기 누락, 빈 근접 자료는 총합 미확인으로 남긴다. 모든 후보가 같은 모형으로 계산될 때 최소 추정 대기 후보를 선택하고, 단축은 목표 거리에 가장 가까운 후보 대비로 표시한다. 화면에 주기 기반·빨간불 50% 가정을 명시한다. 아래 실제 보행현시·범위 검증 미완료 설명은 여전히 유효하며, 이 모형이 이를 해결한 것은 아니다.

- 출처: 서울특별시, [서울시 교차로 및 횡단보도 시설·위치정보](https://data.seoul.go.kr/dataList/OA-23081/F/1/datasetView.do).
- 원본: `서울시 교차로 및 횡단보도 시설·위치정보_20260824.xlsx`, 공개 2026-09-07, 공공누리 1유형.
- 21,776행 중 좌표 누락 1행 제외, 21,775행 포함. SHA256 및 변환 근거는 JSON 메타데이터에 기록.
- `Scripts/prepare_crosswalks.py`가 pyproj로 평면좌표를 WGS84로 변환한다. 원본 Excel 경로를 인자로 전달한다.
- OA-23081 자체에는 좌표계가 명시되지 않았다. 같은 T-GIS 계열의 [공식 횡단보도 데이터](https://data.seoul.go.kr/dataList/OA-15554/S/1/datasetView.do)와 [서울시 위치도](https://bigdata.seoul.go.kr/data/selectSampleData.do?sample_data_seq=105)의 EPSG:5186을 적용했다. 시청입구 등 변환값은 해당 지역과 부합하지만, 정확한 횡단/신호 결합 전 추가 확인 필요.

## 동작과 한계

선택한 보행 경로의 각 polyline 선분과 횡단보도 대표점 사이 거리 12m 이내를 후보로 표시한다. 출발점부터 경로 누적거리 순으로 정렬한다. 분리된 경로 구간 사이에는 가상 연결선을 만들지 않는다. 동일 시설은 경로 거리 40m 미만으로 이어지는 검출을 한 방문으로 합치고, 더 뒤에 다시 지나면 별도 방문으로 유지한다. 지도에는 첫 80개 방문, 목록에는 전체 방문을 표시한다.

대표점만으로 실제 횡단, 평행 보도 통행, 입체교차를 판별할 수 없다. 후보 0건은 횡단보도 0곳을 의미하지 않는다. 도달 시간은 geometry 누적거리 × 페이스이며 신호 대기는 미반영이다. 일반 코스 생성은 전국 가능하지만 위치 후보 데이터는 서울만 포함한다.

T-GIS 교차로관리번호와 T-Data itstId의 동일성은 확인되지 않았다. 방향별 신호 필드 연결도 미확인이다. 따라서 현재 후보를 실시간 보행신호와 자동 결합하거나 대기시간 0으로 해석하지 않는다. 이 단계는 API 호출 없이 동작한다.

## 검증

`xcrun swiftc -parse-as-library GrunnY/CrosswalkMatcher.swift Tests/CrosswalkChecks.swift -o /tmp/GrunnY-CrosswalkChecks`

`/tmp/GrunnY-CrosswalkChecks`

합성 geometry로 순서, 이탈점 제외, 반복 방문, 분리 구간, 중복 좌표를 검증하고 실제 번들 개수/ID 중복도 확인한다. 합성 좌표는 앱 데이터에 포함하지 않는다.

## 신호 조회 화면 연결 (2026-09-17)

서울시 [교차로 MAP CSV](https://t-data.seoul.go.kr/dataprovide/trafficdataviewfile.do?data_id=10144)의 공개 다운로드는 1,000행이다. 제공 파일명은 `v2xCrossroadMapInformation_20241114_final.csv`, 원본 해시는 `SeoulIntersections.json`에 기록했다. 2024년 자료이므로 현재 운영 범위·식별자 변경 가능성은 남아 있다.

실제 대조에서 T-GIS와 T-Data 번호가 다른 체계임을 확인했다. 예: T-GIS 역삼역 160 → T-Data 역삼역 2217. 번호 동등 조인을 사용하지 않는다. 보행등 ‘유’ + 공백 제외 이름 완전 일치 + 80m 이내에 유일한 T-Data 교차로가 있는 경우만 **교차로 후보**로 제시한다. 다중 후보, 미일치, 먼 좌표, 보행등 미설치는 자동 연결하지 않는다. 이 조건도 공식 대응표나 방향 검증을 대체하지 않으므로 대기시간 계산에 사용하지 않는다.

후보 목록의 링크는 해당 itstId가 미리 입력된 조회 화면을 연다. 자동 호출하지 않고 사용자가 키를 입력하고 조회를 눌러야 한다. 성공 응답은 앱 메모리에서 공유하여 다시 횡단보도 목록으로 돌아와도 확인 가능하다. 실패·429는 기존 관측값을 지우지 않는다. 관측값은 조회시각·원본전송시각과 함께 과거 기록으로 표시하며 실시간 카운트다운하지 않는다. 방향 전체를 보여주되 특정 횡단보도의 신호라는 주장을 하지 않는다.

## 방향과 관측값 해석 (2026-09-17 추가)

서울시 [공식 1쪽 설명서](https://t-data.seoul.go.kr/dataprovide/download.do?id=10000)의 그림·본문을 확인했다. API 방향은 차량 진입 기준이며, 직진 차량의 오른쪽 횡단보도에 같은 신호그룹을 부여한다. 지도상 서쪽→nt, 북쪽→et, 동쪽→st, 남쪽→wt. 대각선은 북서→ne, 북동→se, 남동→sw, 남서→nw. 차량 방향 구간은 그림의 27.5/62.5/117.5/152.5/207.5/242.5/297.5/332.5도 경계를 따른다.

앱은 교차로 중심에서 횡단보도 대표점으로의 방위각에 이 규칙을 적용해 **방향 후보**를 표시한다. 이는 비정형 교차로 현장 검증이나 공식 횡단보도별 대응표가 아니다. 중심에서 8m 미만, 80m 초과, 방향 경계 5도 이내, 같은 관리 교차로에 같은 방향의 보행등 시설이 여러 개면 선택하지 않는다. 선택 불가 시 전체 신호를 유지한다. 아직 경로 대기시간 계산에 사용하지 않는다.

[API 명세](https://t-data.seoul.go.kr/dataprovide/trafficdataviewopenapi.do?data_id=10339)의 설명은 잔여값을 1/10초로 정의한다(Cs라는 이름만 보고 1/100초로 변환하지 않는다). 0 이상 36000 미만 유한값만 /10하여 관측 당시 잔여시간으로 표시하고, 범위 밖은 해석 불가로 남긴다. 이 상한은 보수적 구현 제한이며 제공기관이 모든 특수값을 정의했다는 뜻이 아니다.

trsmUtcTime은 공식 예제의 밀리초 Unix timestamp를 사용한다. 30초 신선도 기준과 미래 5초 허용은 앱의 보수적 표시 기준이며 제공기관 보장값이 아니다. 원본 시각 미확인/과거/미래를 구분한다. 화면의 타이머는 시각 표시만 갱신하며 네트워크를 호출하지 않는다.

미래 예측에는 신호 주기·현시 순서·옵셋·유효 운영시간 및 실제 횡단 방향 검증이 추가로 필요하다. 현재 Fusion API만으로 반복 주기를 임의 생성하지 않는다. 추가 조사한 [경찰청 교차로계획정보서비스](https://www.data.go.kr/data/15056569/openapi.do)는 서울 L01과 getPlanCROPInfo의 주기·옵셋·A/B링 현시 필드를 명세한다. 개발/운영 자동승인이지만 별도 공공데이터포털 인증키가 필요하다. 아직 인증 호출·실제 보행현시 연결은 검증되지 않았으므로 키 발급만으로 추천 기능이 완성된다고 볼 수 없다.

추가 테스트:

`xcrun swiftc -parse-as-library GrunnY/SeoulSignalRow.swift GrunnY/SignalIntersectionLink.swift GrunnY/CrosswalkMatcher.swift Tests/SignalInterpretationChecks.swift -o /tmp/GrunnY-SignalInterpretationChecks`

`/tmp/GrunnY-SignalInterpretationChecks`

## 전체 계획 수집과 출발일 후보 선택

운영·요일·특수일·예약계획의 4개 endpoint를 순서대로 수집한다. 페이지당 100건, 종류별 최대 20페이지, 요청 간 0.5초 간격이다. 페이지 번호·총건수·전체페이지 변화, 중복 행, 마지막 건수 불일치는 실패로 처리하며 완성되지 않은 결과로 이전 전체 결과를 덮어쓰지 않는다. 성공한 4종 전체 결과는 앱 메모리에서 유지한다. 화면 원본 미리보기는 첫 100건만 표시하지만 수집 데이터는 전체이다. 키는 메모리의 입력화면에만 보관한다.

한국 시간 기준 월요일1~일요일7은 경찰청 [코드정의서](https://www.utic.go.kr/upload/sig_code.hwp)의 PLAN_DY 표를 사용했다. 특별일이 없는 날짜의 요일계획을 고르고, 그 계획에서 출발 시각 이하인 가장 늦은 적용 시각을 선택한다. 번호 체계는 경찰청 데이터 내부에서만 조인한다. 중복·누락·주기/옵셋 오류·A/B링 합계 불일치는 후보를 고르지 않는다.

해당 날짜 특수일 계획이 있거나 비활성(0) 이외 예약 제어가 하나라도 등록돼 있으면 우선순위/예약조건 검증을 위해 선택을 보류한다. 자정 이후 당일 첫 계획 시작 전에도 전날 계획을 임의로 가져오지 않는다. 따라서 이 기능은 모든 날의 적용계획을 완벽히 결정하는 기능이 아니며, 실제 제어기 동작과 보행현시 대응까지 검증된 예측도 아니다.

`xcrun swiftc -parse-as-library GrunnY/SeoulPlanPage.swift GrunnY/SeoulPlanCollection.swift Tests/PlanCollectionChecks.swift -o /tmp/GrunnY-PlanCollectionChecks`

`/tmp/GrunnY-PlanCollectionChecks`

기존 SeoulPlanChecks도 컴파일 소스에 SeoulPlanCollection.swift를 함께 포함한다.

## 경찰청 기반정보 연결 (2026-09-17)

[경찰청 교차로기반정보서비스](https://www.data.go.kr/data/15056721/openapi.do)의 공식 `기술문서_교차로기반정보서비스_수정(230925).docx`를 확인했다. 현재 포털 HTTPS 주소를 사용한다.

- `CrossRoadInfoService/getCrossRoadInfoList`: `CrossRoadInfoList` 레코드의 지역·교차로 번호·이름·좌표.
- `CrossRoadInfoService/getCrossRoadInfoDetail`: `CrossRoadInfoDetail` 레코드의 `MAP_NO`, A/B 1~8 `PHASE_CONF_CD`.
- 방향코드 1자리 S=직진, L=좌회전, P=보행자, 2~4자리 진입각, 5~7자리 진출각. 앱은 정확히 7자리 ASCII 형식과 0..<360 각도만 해석하고 다른 코드는 원본 그대로 표시한다. 이 범위 밖 값을 지원하지 않는 것은 보수적 구현 제한이다.
- 문서의 시청 예제는 차량 S/L 코드뿐이다. 실제 조회에 P가 없을 수 있으며, 차량 현시로 보행시간을 만들어내지 않는다.

운영계획 화면의 ‘기반정보 수집’으로 현재 수집된 계획의 검색어를 사용해 두 API의 모든 페이지를 순서대로 읽는다. 동일 공공데이터포털 키 입력란을 사용하되, 기반정보 서비스의 별도 활용 승인이 필요하다. 실패 시 기존 계획·기반정보를 보존하고 재시도하지 않는다. 키를 파일/로그에 저장하지 않는다.

서울 L01의 경찰청 `INT_NO`가 같고 기반 교차로가 유일한 경우만 구성을 함께 표시한다. 맵은 모두 표시하고 임의로 현재 적용 맵을 고르지 않는다. T-GIS/T-Data 번호와 동일시하지 않는다. 각도 기준축·횡단보도별 대응·적용 맵·보행 녹색/점멸 구간·옵셋 기준시각은 아직 검증되지 않았다. 따라서 경로 대기시간 예측이나 추천 점수에는 반영하지 않았다.

인증된 기반정보 응답은 아직 이 개발 환경에서 확인하지 못했다. 공식 XML 형태 fixture를 이용한 기반정보 파싱·코드 해석·번호 연결·중복 거부·URL 키 인코딩 검사 및 기존 계획 회귀 검사가 통과했고 iOS Simulator 빌드가 성공했다.

`xcrun swiftc -parse-as-library GrunnY/SeoulPlanPage.swift GrunnY/SeoulPlanCollection.swift Tests/PhaseBasisChecks.swift -o /tmp/GrunnY-PhaseBasisChecks`

`/tmp/GrunnY-PhaseBasisChecks`

## 누적 대기 계산기와 코스 분석 화면 (2026-09-17)

`SignalWaitEstimator.swift`는 **확정된 횡단 연결 및 보행 진입 가능 구간으로 정규화된 데이터**를 입력받는 계산기다. 주기 기준은 절대 Date이며, 유효 시작/종료 시각과 녹색 구간을 명시해야 한다. A/B링 전체 시간을 보행 녹색 시간으로 간주하거나, raw offset의 기준을 자정이라고 가정하지 않는다. 녹색 구간은 시작 포함/종료 제외이며 점멸 구간을 제외해야 한다. 주기 끝을 넘는 구간은 두 구간으로 나눠 입력한다.

경로 순서대로 `거리 / 1000 × 페이스 + 이전 대기 합계`를 계산한다. 다음 녹색이 계획 유효기간을 넘으면 예측을 중단한다. 중간 신호를 해석하지 못하면 이후 ETA와 전체 대기시간도 미확정이다. 후보가 0개여도 데이터의 전체 범위를 검증하지 못했다면 대기 0초를 출력하지 않는다. 모든 후보가 계산 가능할 때에만 대기 합계로 최소값을 고르고, 동률이면 목표 거리 차이를 사용한다.

현재 **실제 데이터 adapter는 아직 완성되지 않았다**. RouteSignalAssessment는 기존 API 자료의 수집 여부와 P 코드 존재 여부를 진단할 뿐, 모든 근접 후보를 확인된 횡단으로 바꾸거나 신호 타이밍을 생성하지 않는다. 따라서 운영 앱의 대기시간과 신호 기준 추천은 현재 모두 보류된다. 계산기 테스트에만 합성 데이터를 사용하며, 앱 데이터로 포함하지 않는다. 이번 구현이 실제 신호 최적화 기능의 완료를 의미하지 않는다.

앱의 ‘코스별 신호 분석’에서 후보별 거리·대기 제외 시간·계산 보류 이유를 확인하고 운영계획 조회 화면으로 이동할 수 있다. 출발 ‘지금’의 기준시각을 경로 요청 시 한 번 저장하므로 화면을 다시 열어도 바뀌지 않는다. 횡단보도 후보는 순환 코스마다 보관한다.

검증:
- SignalWaitChecks: 녹색 시작/종료, 주기 경계, 기준시각 이전, 계획 만료, 누적 ETA, 미확인 이후 ETA 중단, 미확인 후보 추천 제외, 대기순/거리 동률 정렬 통과.
- RouteAssessmentChecks: 계획 미수집, 기반 미수집, P 없음, 후보 없는 경로 모두 총 대기 미확정 통과.
- 기존 횡단보도·신호 파싱·방향·단위·신선도 테스트 통과.
- Simulator 실제 MapKit 서울 약 0.62 km 경로와 8개 근접 후보, 신호 분석 화면·미수집 이유 표시를 확인.
- 사용자 시뮬레이터 조작이 감지되어 이후 UI 자동조작을 중단했다. 이번 턴에는 GPS 러닝 시작/종료와 잠금 중 기록을 끝까지 검증하지 않았다. 실제 기기 잠금 중 GPS 검증도 미완료다.

남은 외부 조건: 기반정보 실제 인증 응답 확보, 확정된 횡단보도/보행신호 대응, 적용 맵·보행 녹색/점멸 구간·옵셋 기준시각 확인. 이 근거가 있어야 실제 자료 adapter를 구현하고 대기시간 추천을 활성화할 수 있다. 원 요청의 AI 경유지 생성 또한 현재 미연결이며 기존 후보 생성은 결정적인 경유지 탐색 알고리즘이다.

## 서울 보행현시 탐색

운영계획 화면에 ‘서울 보행현시 검색’을 추가했다. 공식 기반정보 명세에서 선택 항목인 srchCRNm을 생략하고 서울 L01의 getCrossRoadInfoDetail을 페이지당 100건, 최대 100페이지로 조회한다. 요청 간 1초 간격이며 자동 재시도하지 않는다. 기존 계획 수집의 20페이지 제한은 유지한다.

P + 3자리 진입각 + 3자리 진출각으로 해석 가능한 항목만 검색하며 교차로 ID로 묶어 맵 번호와 현시 수를 표시한다. 중단/취소 시 이전에 확인한 페이지는 부분 결과로 표시한다. 전체 페이지가 검증되어야 전체 완료라고 표시하며, 0건 결과는 실제 보행신호 부재를 뜻하지 않는다. 찾은 교차로를 선택하면 해당 이름의 4종 계획 수집 후 기반정보 조회를 이어서 수행한다. 동일 이름이 여러 번호에 대응해도 선택한 경찰청 ID를 우선한다.

PhaseSearchChecks는 지역 필터, S/L 제외, 비정상 P 코드 제외, 맵/교차로 그룹, 미완료 상태, 검색어 생략 및 키 인코딩, 검색용 페이지 제한을 검사한다. 실제 인증키는 앱 입력 메모리에만 있으므로 개발 도구에서 서울 전체 API 검색을 실행한 것은 아니다. 실제 P 교차로가 발견됐다고 주장하지 않는다.

## 인증 검색 실행 결과 및 페이지 처리 수정

실행 중인 iPhone 17 시뮬레이터에서 사용자가 입력해 둔 키로 ‘서울 보행현시 검색’을 실제 실행했다. 총 460건으로 안내됐으며 첫 4페이지 400건에서 P 후보는 0곳이었다. 마지막 페이지에서 누적 건수가 서버 총건수와 달라 중단되었다. 당시 코드는 검증 전에 nextPage를 증가시켜 ‘6페이지 실패’로 표시했고 화면에는 직전 400건만 남겼다. 정확한 마지막 응답 건수는 당시 화면으로 확인하지 못했다.

수정: 페이지 검증 성공 후에만 행과 커서를 반영한다. 건수 불일치에는 응답 페이지·해당 페이지 행수·누적 행수·서버 총건수를 표시한다. P 검색은 파싱에 성공한 마지막 응답도 ‘부분 결과’로 표시하지만 완전한 수집으로 인정하지 않는다. 순차 계산용 계획 수집은 여전히 불완전한 자료를 사용하지 않는다. 중단 페이지의 원인·전체 P 부재는 아직 확정할 수 없다.

원래 실행 중인 앱의 인증키를 유지하기 위해 개발 도구로 앱을 강제 재설치하지 않았다. UI에서 별도 교차로 입력을 시도했지만 입력/클립보드 읽기 시간초과로 진행하지 못했다. 수정본의 인증 검색 재실행은 미완료다.


## 정확성 재점검 (2026-09-17)

UTIC 공식 신호 개방 안내 https://www.utic.go.kr/guide/utisRefSig.do 는 제공 지역을 인천·대전·대구로 설명한다(페이지의 수정일 표기는 2023년). 기존 서울 L01 조회 성공과 안내 범위 사이에 차이가 있으므로 서울 응답의 현행 운영 여부·지원 범위를 제공기관에 확인하기 전 운영 데이터라고 단정하지 않는다. 조회 성공, P 코드 발견 또는 정적 계획 존재만으로 실시간 제어기 동작을 검증한 것으로 보지 않는다.

현재 신호 최적화 미완료 상태를 유지한다. 실제 횡단 연결, 보행 진입/점멸 구간, 절대 주기 기준시각, 계획 적용 범위·현행성이 확보되지 않아 실제 adapter를 활성화하지 않았다. 결과 화면에 신호 분석 진입점을 복원했다. coverageVerified가 false일 때는 누락된 앞 횡단의 대기 영향을 알 수 없으므로 총합뿐 아니라 개별 ETA·대기도 출력하지 않도록 수정하고 회귀 검사를 추가했다.


신호등 미설치 회피 선호: 생성된 거리 ±5% 후보 전체에 근접 시설이 있고 설치 여부가 모두 유/무인 경우, 무 시설 근접 방문 횟수를 우선 최소화하고 동률은 목표 거리 차이로 정렬한다. 빈 후보나 설치 여부 미확인이 하나라도 있으면 자료 누락을 0으로 취급하지 않고 거리 기준으로 보류한다. 이는 근접 대표점 기반 휴리스틱이며 실제 횡단 회피·완전한 자료 범위·신호 안전성을 보장하지 않는다. 현재 탐색한 최대 3개 후보 내 선택에만 적용하며 MapKit 경로 자체를 강제로 우회시키지 않는다.

## 실시간 연결 오류 정정 및 선택 연결

임시 liveTiming 구현이 관측 잔여값+1을 주기로 만들고 모든 구간을 녹색으로 설정하여 녹색 종료 후에도 0초를 산출할 수 있었으며, 좌표 기반 추정 연결을 전체 횡단 검증으로 잘못 승격했다. 이 구현은 제거했다. 관측 한 건에서 주기·다음 보행 진입 가능 구간을 만들지 않는다. RouteSignalAssessment의 실제 자료 경로는 다시 coverageVerified=false이며, 신호 최적화가 완성됐다는 이전 설명은 유효하지 않다.

RoutePlanner.applySignalAssessments를 코스 탐색 완료 및 분석 화면 종료에 연결했다. 현 검색의 후보 ID·거리와 일치하고 모든 후보의 계산이 완료된 경우에만 대기시간 최소 후보를 실제 선택한다. 결과 화면은 계산값이 있는 경우에만 대기를 합한 예상 시간을 표시한다. 현재 검증된 실제 자료 adapter가 없으므로 운영 앱에서는 이 조건을 만족하지 않는다. 분석 화면에서 횡단보도 후보의 API 조회 화면으로 이동할 수 있게 복원했다.

공식 T-Data 안내(https://t-data.seoul.go.kr/)에 따르면 기존 Fusion API는 단기 이력을 포함한다. 최신 전용 서비스는 https://t-data.seoul.go.kr/dataprovide/trafficdataviewopenapi.do?data_id=10381 이며 endpoint는 v2xSignalPhaseTimingFusionCurrentInfo이다. 새 서비스도 현재 상태·잔여시간이며 반복 신호계획 제공으로 해석하지 않는다. 이번 작업에서 새 서비스의 인증키·활용 승인이나 인증된 원본 응답을 확보한 것은 아니다.

회귀 검사: 합성 일정에서 대기 70초 후보보다 거리상 더 먼 대기 10초 후보가 우선됨, 합계 불일치·완료시각 누락 거부, 최신 초록불 관측만으로 전체 횡단 검증 승격 금지. 실제 현장 예측 정확도 검증과 구분한다.
