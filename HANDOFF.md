# GrunnY 작업 인수인계

작성일: 2026-09-18 (한국 시간)

이 문서는 지금까지의 대화를 작업 재개에 필요한 결정사항으로 정리한 문서다. 대화 전문이 아니며, 최신 요구사항이 과거 요구사항보다 우선한다. 현재 코드와 확인된 동작, 추정 및 미확인 사항을 구분했다.

## 1. 프로젝트와 목표

- SwiftUI 네이티브 iOS 러닝 앱 **GrunnY**.
- 프로젝트: `/Users/choeseojin/Documents/Project/AI 원티드 해커톤/GrunnY`
- Xcode 프로젝트/스킴: `GrunnY.xcodeproj` / `GrunnY`.
- 핵심 목표: 출발지·거리·페이스를 입력하여 순환 러닝 코스를 만들고 신호 대기가 적은 코스를 비교한다.
- 혼자 빠르게 완성해야 하는 해커톤 앱. 과도한 아키텍처나 불필요한 기능 추가 금지.
- 처음에는 전국 지도와 대구 신호 데이터를 논의했지만, 현재 장소 검색과 신호 데이터 활용 범위는 서울 중심이다. 대구는 사용자 요청으로 제외했다.

## 2. 사용자의 작업 방식과 강한 선호

1. **기존 디자인을 절대 해치지 말 것.** 색상·폰트·여백·지도 크기·수치 배치·하단 버튼을 임의로 재디자인하지 않는다.
2. 새로운 기능은 기존 화면에 최소한으로 붙인다. 카드로 전면 재구성했다가 거절당했고 원래 화면으로 복원했다.
3. 사용자에게 필요한 말만 보여준다. API 원본, 현시 코드, 운영계획, 복잡한 기술 설명은 일반 사용자 화면에서 제거한다.
4. 필요 없는 UI를 숨기는 것과 프로젝트의 기존 기능을 삭제하는 것은 다르다. 진단·데이터 관련 소스는 보존한다.
5. **코드를 수정하면 빌드까지 한다. 앱 실행·설치는 사용자가 직접 한다.** 과거 실행 요청은 이후 철회됨.
6. 숫자나 기능을 과장해서 설명하지 않는다. 특히 신호 예측은 실제 검증된 데이터와 가정을 분리한다.
7. 별도 요청 없이 서브에이전트 사용하지 않는다.
8. 작업 트리가 수정·미추적 파일을 포함한 상태다. 사용자 변경을 덮어쓰거나 `git reset` 등으로 되돌리지 않는다. 과거 변경이 사용자에 의해 되돌아간 적이 있어 수정 전 파일을 확인한다.

## 3. 현재 사용자 흐름

출발지 선택 → 거리·페이스·출발 시각 설정 → 코스 탐색 → 코스 1/2 선택 → ‘이 코스로 달리기’ → 3/2/1/START → 러닝 화면 → 종료 확인 → 결과 → 완료 시 첫 출발지 화면.

### 출발지 선택

파일: `GrunnY/StartLocationView.swift`

- 첫 화면에만 GrunnY 로고를 표시한다.
- MapKit 장소 검색, 서울 결과만 필터링한다. 검색 영역 설정만으로 서울을 보장하지 않아 행정구역과 국가도 검사한다.
- 검색은 450ms 디바운스, 작업 취소 및 세대 토큰으로 이전 응답을 무시한다.
- 로딩 문구는 제거했고 스피너만 가운데 표시한다.
- 검색 결과 없음 문구는 검색창 아래 적당한 간격을 두고 가운데 정렬한다.
- 결과 선택 시 지도와 출발지 이름을 갱신한다. 이후 ‘다음’으로 설정 화면에 간다.
- 최근 문제: 검색 결과를 한 번 눌렀는데 재검색되어 두 번 눌러야 했음.
  - 검색어 전체 `onChange` 대신 TextField의 사용자 입력 Binding에서만 검색한다.
  - 프로그램이 선택한 장소명을 검색창에 넣는 동작은 검색을 시작하지 않는다.
  - `StartPlaceSearch.activeQuery`로 동일 검색어의 중복 요청과 결과 초기화를 막는다.
  - 선택 후 늦은 submit이 검색을 다시 열지 않도록 처리했다.
  - 빌드는 확인했지만 실제 키보드 한글 조합·탭 동작은 기기 재확인이 필요하다.
- 지도 탭은 역지오코딩: 장소명이 있으면 이름, 없으면 주소를 표시한다. 선택 좌표는 실제 탭 좌표를 유지한다.
- 지도 안에만 현재 위치 버튼이 있다. 별도의 ‘현재 위치에서 시작하기’ 버튼은 삭제했다.
- 현재 위치 아이콘은 회색 원형 바탕과 조준선 모양이다.

### 목표 설정 및 탐색 화면

파일: `GrunnY/ContentView.swift`, `GrunnY/CourseSearchLoadingView.swift`

- 거리·페이스 증감과 출발 시각 휠을 사용한다.
- 허용 거리 오차는 **±5%**.
- 하단 버튼 텍스트는 가운데 정렬, 장식 화살표 없음.
- 탐색 화면은 원래 곡선 순환 경로 모양과 움직이는 진한 구간 애니메이션을 사용한다.
- 불필요한 설명·칩·카드를 추가하지 않는다.
- 단계 문구는 실제 처리 단계와 연결되어 있다. 실시간 신호 API를 자동 조회한다는 의미는 아니다.

### 코스 결과 화면 — 최신 디자인 결정

파일: `GrunnY/ContentView.swift`, `GrunnY/GrunnYDesign.swift`

- 제목 ‘달릴 길을 찾았어요.’, 실제 지도, 기존 수치 배치, 하단 그라데이션 버튼을 유지한다.
- 큰 선택 카드 디자인은 **폐기**했다.
- 지도 아래 **코스 1 / 코스 2 탭이 너비를 반씩 차지**한다. 가로 스크롤 아님.
- ‘추천’ 문구는 탭에 붙이지 않는다. 선택 탭은 기존 브랜드 색과 밑줄로 표시한다.
- 최초 선택은 항상 **코스 1**. 계산 후 `planner.selectCandidate(0)` 호출.
- 탭 변경 시 지도 범위 및 표시 수치가 바뀐다.
- ‘예상 신호 대기’ 바로 위 구분선은 삭제했다. 그 아래 수치 영역과의 구분선은 유지한다.
- 큰 초 숫자는 **선택한 코스의 총 예상 신호 대기시간**이다.
  - 과거에는 기준 코스 대비 단축 시간이어서 0초가 자주 나왔고 사용자 혼란이 있었음.
  - 현재 `selectedSignalWait`가 `planner.selectedSignalEstimate.totalWait`를 표시한다.
  - 다시 ‘절약 시간’이나 ‘코스 간 차이’로 바꾸지 말 것.
- 거리, 예상 총시간, 목표 페이스, 주변 신호 횡단보도 수를 기존 배치로 표시한다.
- 총시간은 `(거리 km × 목표 페이스 초/km + 추정 대기 초)`를 분으로 올림하여 표시한다.
- 일반 결과 화면의 상세보기 버튼은 제거했다.
- `RouteComparisonView.swift`는 별도 도구 화면에서 사용하는 간단한 코스 비교로 남아 있다. 원본 API 분석 UI 소스도 보존되어 있다.

### 시작 카운트다운

파일: `GrunnY/RunCountdownView.swift`, `GrunnY/ContentView.swift`

- **‘이 코스로 달리기’ 버튼**에 연결되어 있다. 별도 ‘러닝 시작’ 버튼을 의미하지 않는다.
- 위치 상태가 `canStartRun`일 때 전체 화면 표시.
- 배경 RGB `(0.294, 0.918, 0.839)`, 흰색 중앙 텍스트.
- 3 → 2 → 1은 1초 간격, START는 600ms 표시.
- 크기와 투명도 전환, 동작 줄이기에서는 투명도만 전환한다.
- 카운트다운 완료 후 `location.startRun()` 호출. 카운트다운 시간은 러닝 기록에 포함하지 않는다.
- 같은 fullScreenCover 안에서 카운트다운에서 러닝 화면으로 전환한다.
- 앱이 비활성화되면 카운트다운을 취소한다. 화면 해제 시 Task 취소.
- 위치 권한/위치 준비가 부족하면 기존 권한 및 위치 요청 흐름을 따른다.

### 러닝/완료 화면

파일: `GrunnY/GrunnYDesign.swift`, `GrunnY/LocationManager.swift`

- 실제 위치 기반 거리·시간·페이스와 지도 표시.
- 가짜 ‘300m 앞 좌회전’이나 실제 구현되지 않은 음성 안내를 확정적으로 표시하지 않는다.
- 러닝 종료 시 커스텀 확인 팝업: ‘계속 달리기’ / ‘러닝 종료’.
- 결과는 거리·평균 페이스·시간·페이스 흐름. 칼로리는 사용자 요청으로 제거했다.
- 완료를 누르면 맨 첫 출발지 선택 화면으로 돌아간다.
- 영구 기록 저장이 구현되어 있다고 약속하지 않는다.

## 4. 코스 탐색 구현과 성능

파일: `GrunnY/RoutePlanner.swift`

- MapKit 보행 경로 사용. AI 생성 경로나 고도 최적화가 구현된 것은 아니다.
- 출발지 → 경유지 A → 경유지 B → 출발지, 총 세 구간을 순차 요청한다.
- 세 방향을 시도하고 방향마다 최대 두 번 거리 보정한다. 최악에는 18회 경로 요청.
- 요청마다 350ms 간격을 유지한다. 네트워크 및 MapKit 응답 시간이 추가된다.
- 거리 ±5%, 연결된 순환 경로, 일정 면적 조건을 확인한다.
- **정확히 두 후보가 확보됐을 때만 결과 화면을 제공한다.** 부족하면 출발지·거리 변경 안내. 한 코스를 복제해 두 개로 보이게 하지 않는다.
- 최신 성능 수정:
  - 유효 후보 두 개를 찾으면 `break search`로 추가 방향 탐색을 중단한다.
  - 로딩 단계 문구를 보여주려고 넣었던 500ms 대기 두 개(총 1초)를 삭제했다.
  - 기존 API 요청 사이 350ms는 유지한다.
- 초기 후보 정렬은 시설 자료를 비교할 수 있으면 신호등 미설치 근접 시설이 적은 후보 우선, 이후 목표 거리와의 차이 순이다.
- `applySignalAssessments`는 예상 대기가 적은 후보를 계산·선택하지만, 현재 메인 화면은 사용자 지시로 계산 직후 코스 1을 기본 선택한다. 따라서 **처음 보이는 코스 1이 언제나 신호 대기 최소라고 설명하면 안 된다.**
- 두 후보는 탐색으로 얻은 후보이며 서울 전체의 전역 최적 코스라는 보장은 없다. 경로 중복 정도를 정교하게 평가하는 로직도 별도 확인 필요.

## 5. 신호 대기 추정 — 가장 중요한 한계

### 현재 실제 사용 모델

파일: `RouteSignalAssessment.swift`, `SignalWaitEstimator.swift`

사용자가 정확한 예측을 원했으나 데이터 연결을 검증하지 못했고, 이후 **‘그냥 주기로 계산’, ‘대충 해줘’**라고 추정 모델을 수용했다.

- 전체 신호 주기 C 중 빨간불이 절반이라는 **가정**: R = C / 2.
- 무작위 도착에 대한 평균 대기: `R² / (2C) = C / 8`.
- 예: 주기 120초 → 빨간불 60초 가정 → 평균 대기 15초.
- 빨간불 50%는 실제 확보한 보행신호 비율이 아니다.
- 우선 해당 교차로의 적용 가능한 운영주기를 사용한다.
- 현재 적용 계획이 불확실하면 해당 교차로에 등록된 유효 주기의 평균으로 추정한다.
- 해당 교차로 자료가 없으면 수집된 다른 교차로 평균을 사용한다.
  - 교차로별 평균을 먼저 낸 뒤 교차로 간 평균을 내어 계획 행 수가 많은 교차로의 과도한 가중을 막는다.
- **수집한 주기가 전혀 없으면 기본 120초를 가정**한다.
- 신호등 미설치(`무`) 시설은 대기 계산에서 제외한다. 해당 횡단보도를 실제 경로에서 완전히 제거하는 것은 아니다.
- 설치 여부 미확인은 신호등 있음으로 가정한다.
- 경로 근처에서 횡단보도가 검출되지 않으면 현재 모델은 자료 기준 0초를 낸다. 실제 신호가 없다는 증거는 아니다.
- 잘못된 거리·페이스·좌표 등 입력 오류는 무조건 숫자로 덮지 않는다.
- 같은 조건·같은 시설 수·같은 기본 주기를 쓰면 결과가 반복되는 것이 자연스럽다. 임의로 랜덤 초를 넣으면 안 된다.

### 현재 관측값을 제한적으로 연결한 부분

파일: `SignalTimingConnection.swift`

- 방향에 대응되는 최신 관측이 30초 이내인지 확인한다.
- 관측 시각 + 잔여시간으로 현재 상태의 종료 예정 시각을 구한다.
- 예상 도착이 현재 관측된 진입 가능 초록불 구간 안에 있고 종료 전 2초 여유가 있으면 해당 대기를 0초로 반영한다.
- 현재 상태 이후 다음 주기를 반복 예측하지 않는다.
- 빨간불 잔여시간을 전체 빨간불 길이나 전체 주기로 사용하지 않는다.
- 방향/횡단보도 연결은 지리적 후보 연결이며 현장 검증 완료가 아니다.

### 정확한 미래 신호 예측에 부족한 것

1. 실제 경로가 건너는 횡단보도와 신호 방향의 정확한 대응.
2. 그 횡단보도의 보행 진입 가능 초록불 시작·종료 및 점멸 구간.
3. 해당 시각에 실제 적용되는 신호 맵과 주 현시·제어 상태.
4. 실시간 값과 운영계획의 현장 비교 검증.

일반적인 자정/옵셋 기준을 알아도 개별 횡단보도 초록 시작이 확정되는 것은 아니다. 사용자가 공유한 시청(경찰청 교차로 2904) 기반정보 화면에는 S/L 차량 현시가 있었고 P 보행현시는 없었다. 차량 현시 시간을 보행 초록 시간으로 취급하면 안 된다.

## 6. 데이터/API 상태

- 서울 T-Data 신호 상태·잔여시간 Fusion API, 운영계획, 기반정보를 연결했다.
- 사용자에게 429 호출 제한 및 기반정보 403이 발생한 적이 있고, 이후 사용자 기기에서 조회 성공을 확인했다.
- 인증키는 이 문서에 넣지 않는다. API 응답은 사용자가 앱에서 직접 수집하며 자동 전체 수집이 구현되었다고 가정하지 않는다.
- 현재 화면의 키/응답은 메모리 기반 상태이며 재실행 후 영구 저장을 보장하지 않는다.
- 에이전트가 최신 인증 API를 직접 호출하여 전체 데이터 정합성을 검증한 것은 아니다.
- 시설 자료: 서울 횡단보도 약 21,775개, 신호 교차로 MAP 약 1,000개를 번들 자료로 사용한다. 상세 출처/날짜/가정은 `CROSSWALK-DATA.md` 참조.
- 경로와 횡단보도 대표점 약 12m 근접으로 후보를 찾는다. 평행 보도·고가도로 등에서 실제 횡단과 다를 수 있다.
- TGIS, T-Data, 경찰청 교차로 ID를 동일하다고 간주하지 않는다.
- `SignalIntersectionLink.swift`: 이름 및 거리/방향 기반 후보 연결, 모호하면 거절한다.
- `SeoulPlanCollection.swift`: 운영/요일/특수일/예약 계획의 적용 조건 검증. 예약 제어나 맵 미확정인 경우 정확한 적용 계획이 확인되지 않을 수 있다.

참고 공식 자료:

- https://t-data.seoul.go.kr/dataprovide/trafficdataviewopenapi.do?data_id=10339
- https://www.utic.go.kr/guide/utisRefSig.do
- https://data.seoul.go.kr/dataList/OA-23081/F/1/datasetView.do
- https://www.data.go.kr/data/15056569/openapi.do

일부 상세 조사 문서는 `/tmp/grunny-...`에 있었지만 임시 파일이므로 존재를 보장하지 않는다. `CROSSWALK-DATA.md`에는 과거 기록도 있어 최신 설명과 충돌할 경우 현재 코드와 최신 사용자 결정을 우선한다.

## 7. 주요 파일 지도

| 파일 | 역할 |
|---|---|
| `ContentView.swift` | 메인 화면 흐름, 목표, 결과, 두 탭, 대기 표시, 카운트다운 진입 |
| `StartLocationView.swift` | 서울 장소 검색, 지도 선택, 주소, 현재 위치 |
| `RoutePlanner.swift` | MapKit 순환 코스 탐색, 거리 조건, 후보 선택 |
| `CourseSearchLoadingView.swift` | 경로 모양 로딩 애니메이션 및 실제 단계 표시 |
| `RunCountdownView.swift` | 3/2/1/START 전체 화면 |
| `GrunnYDesign.swift` | 색/버튼, 결과 지도, 러닝/완료 화면 |
| `LocationManager.swift` | 위치 권한, 러닝 기록 |
| `RouteSignalAssessment.swift` | 주기 추정과 경로별 예상 대기 |
| `SignalWaitEstimator.swift` | 정확한 윈도 모델 및 평균 모델, 후보 비교 |
| `SignalTimingConnection.swift` | 신선한 관측 구간과 예상 도착의 제한적 연결 |
| `RouteComparisonView.swift` | 도구 화면용 간단한 코스 비교 |
| `MapWorkspaceView.swift` | 기존 지도/진단 기능 |
| `SeoulPlanCollection.swift` | 운영계획 적용 조건 |
| `CrosswalkMatcher.swift` | 경로 근접 횡단보도 후보 |
| `SignalIntersectionLink.swift` | 교차로/신호 방향 후보 연결 |

## 8. 빌드 및 검증

수정 후 빌드하되 앱 실행/설치는 하지 않는다.

```sh
xcodebuild -project GrunnY.xcodeproj -scheme GrunnY \
  -configuration Debug -sdk iphonesimulator \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/GrunnY-design-build \
  CODE_SIGNING_ALLOWED=NO build > /tmp/GrunnY-build.log 2>&1
```

샌드박스에서 Swift 매크로/시뮬레이터 서비스 접근 오류가 생겨 `xcodebuild`는 권한 확장 실행으로 성공했다. 이 환경 문제를 소스 컴파일 오류로 오해하지 않는다.

마지막 코드 수정(탐색 조기 종료/불필요 대기 제거)의 빌드: **성공**, 로그 `/tmp/GrunnY-search-speed.log`.

아래 독립 Swift 검사들이 과거 통과했다. 최신 UI 변경 뒤 전부 다시 실행한 것은 아니다.

```sh
swiftc -module-cache-path /tmp/GrunnY-swift-module-cache \
  GrunnY/SignalWaitEstimator.swift GrunnY/RouteSignalAssessment.swift \
  GrunnY/SignalTimingConnection.swift GrunnY/SignalIntersectionLink.swift \
  GrunnY/CrosswalkMatcher.swift GrunnY/SeoulPlanCollection.swift \
  GrunnY/SeoulPlanPage.swift GrunnY/SeoulSignalRow.swift \
  Tests/RouteAssessmentChecks.swift -o /tmp/GrunnY-route-checks
/tmp/GrunnY-route-checks
```

추가 검사: `Tests/SignalTimingConnectionChecks.swift`, `Tests/SignalWaitChecks.swift` 등. 합성 데이터 기반 테스트이며 실제 신호의 현장 정확도 검증이 아니다.

## 9. 다음 작업자가 먼저 확인할 것

- 최신 요구는 완료된 코드의 인수인계 작성이다. 자동으로 새로운 기능을 추가할 필요는 없다.
- 사용자 기기에서 검색 결과 한 번 탭, 두 코스 탭 전환, 카운트다운 완료/중단을 확인할 여지가 있다. 빌드 성공을 UI 실행 검증으로 표현하지 않는다.
- 탐색 속도는 아직 네트워크, ±5% 제한, 정확히 두 코스 확보 조건의 영향을 받는다. 실제 속도 향상 수치는 측정하지 않았다.
- 초가 반복되는 원인을 숨기지 않는다. 실제 주기 데이터가 연결되지 않으면 기본 주기120초/시설당 평균15초 모델이다.
- 더 정확한 예측 개선을 요청받으면 한 교차로에서 실제 보행 신호 데이터를 검증하는 것부터 진행한다. ‘정확한 미래 신호 최적화가 완성됐다’고 말하지 않는다.
- 표시 수치는 현재 **절약 시간 아닌 총 예상 신호 대기**이고, 초기 탭은 **코스 1**, 탭 이름에 **추천 없음**, 후보는 **두 개**라는 최신 결정을 유지한다.

## 10. 인수인계 작성 이후 수정: 카운트다운 건너뛰기

사용자가 카운트다운과 러닝 화면이 나오지 않고 완료 화면으로 이동한다고 보고했다.

- `ContentView`의 `showsSession`/`showsCountdown` 두 Bool을 제거했다.
- `RunPresentation`을 `fullScreenCover(item:)`로 전달하여 화면을 열 때 카운트다운 여부를 함께 확정한다.
- `RunSessionFlow`가 내부 상태로 카운트다운 → 기록 시작 성공 → 러닝 화면을 연결한다.
- 취소할 때 카운트다운 분기를 먼저 끄지 않는다. 모달 해제 도중 미시작 세션의 완료 화면이 노출되는 것을 방지한다.
- 시작 실패 시 모달을 닫고, 시작 시각이 없는 세션을 결과 화면으로 넘기지 않는다.
- 빌드 성공: `/tmp/GrunnY-countdown-fix.log`. 실제 기기에서 재현/해결 여부를 실행 검증한 것은 아니다.

## 11. 러닝 화면의 다음 길 안내

- `RunGuidance.swift` 추가. `DesignedRunSession`의 기존 지도 확인 카드를 같은 모양의 ‘다음 안내’ 카드로 변경했다.
- 선택 경로의 MapKit 보행 steps/instructions와 GPS의 경로상 투영 위치로 다음 안내 지점까지의 거리를 계산한다. 좌/우회전·유턴은 짧은 문구/아이콘, 그 외는 원본 안내 문구를 유지한다.
- 순환 코스의 시작/종점 및 교차 구간에서 다음 구간으로 크게 건너뛰지 않도록 이전 진행 위치 주변만 탐색한다.
- 오래되거나 오차가 큰 GPS, 경로 이탈, 안내 step 누락 시 방향을 만들어내지 않고 위치/지도 확인 문구를 표시한다. 자동 재탐색은 구현하지 않았다.
- AVSpeechSynthesizer로 다음 안내를 300m 이내와 30m 이내에서 중복을 제한해 읽는다. 종료/화면 해제 시 중지한다.
- 카드 탭으로 지도 확인 기능은 유지한다. 화면이 갱신되는 동안 안내를 처리하며, 잠금 화면/백그라운드 연속 음성 안내까지 검증한 것은 아니다.
- 합성 위치 테스트 `Tests/RunGuidanceChecks.swift` 통과: 순환 코스 시작점, 남은 거리 변화, 큰 위치 점프, 부정확/오래된 GPS, 안내 누락.
- 빌드 성공 `/tmp/GrunnY-guidance.log`. 실기기 보행, 실제 MapKit 현시점 응답의 각 안내 지점, 음성 재생은 현장 검증하지 않았다.

## 12. 사용자 지정 음성 안내 및 실제 기록 저장 (2026-09-18)

- `RunVoice.swift`: 공통 한국어 TTS. 안내가 중첩되지 않도록 직렬 처리하며 방향 안내는 1km 기록보다 우선한다. 중단한 구간 기록은 다시 읽는다. 오래된 방향 안내는 만료/이탈 시 제거한다. 시스템 음성 세션의 믹싱/오디오 중단 처리를 사용한다.
- 카운트다운과 ‘러닝을 시작할게요.’ 음성 연결. 취소 시 음성도 취소한다.
- `RunAnnouncements.swift`: 1km 경계 시각을 GPS 거리 구간에서 보간하여 **직전 1km 페이스**를 계산. 사용자 제공 문구 그대로 누적 km·분·초를 읽는다. 목표 거리 달성은 한 번만 알리고 기록/길 안내를 계속한다.
- 이탈 설정은 사용자가 **30초 간격 · 총 3회**로 답변했다. 5초 지속 이탈 후 최초 안내, 30초 간격 반복 두 번, 다음 30초에도 이탈 상태면 경로 안내 종료. 종료 전 복귀하면 복귀 멘트와 안내 재개. 최대 횟수로 종료된 길 안내는 해당 러닝에서 재개하지 않는다.
- `RunGuidance`는 LocationManager 소유로 옮겼고 GPS 콜백에서 갱신한다. 화면 타이머에만 의존하지 않는다. background audio 모드를 추가했지만 잠금 화면/음악/전화와의 실제 동작은 기기 검증 필요.
- 목적지는 마지막 경로 좌표, 위치 오차와 출발지에서 실제로 멀어졌는지, 경로 진행 및 연속 위치 3개를 함께 검사한다. 순환 코스 시작점에서 즉시 도착 처리하지 않는다. 길 안내가 종료돼도 이동 거리와 목적지 접근을 검사한다. 경로/위치 데이터 자체가 없으면 도착을 만들어내지 않는다.
- 도착 시 음성과 함께 `endRun(reason: .arrival)`로 자동 종료하고 완료 화면으로 전환한다.
- **미션 성공은 현재 live 코드에서 판정하지 않는다.** 허용 페이스/예상 시간 범위가 정의되지 않았고 실제 보행 신호 조건 준수를 검증할 데이터가 없기 때문이다. `RunCompletionEvidence`는 모든 증거가 true일 때만 성공 멘트를 내며, 현재 live 입력은 미확인(nil)이므로 일반 목적지 도착 멘트를 쓴다. 향후 데이터 연결 없이 이를 true로 바꾸지 말 것.
- `RunRecord.swift`: 종료 시 Application Support/RunRecords 아래 개별 JSON을 원자적으로 저장한다. 거리·시각·목표값·종료 사유·좌표 기록 포함. 저장 실패 시 성공 멘트를 피하고 결과 화면에 재시도 제공. 기록 목록 UI는 추가하지 않았다.
- 직접 종료는 지정한 ‘러닝을 종료했어요. 지금까지의 기록을 저장할게요.’ 멘트. 시스템 위치 오류 종료는 별도 중단 멘트.
- 테스트: `RunAnnouncementsChecks.swift`의 구간 보간·중복 방지·이탈 간격/복귀/종료·미션 증거·파일 저장 검사 통과. `RunGuidanceChecks.swift`에 순환 코스 완주 및 연속 도착 확인, 방향 문구 검사를 추가해 통과.
- 빌드 로그 `/tmp/GrunnY-voice.log`. 실제 음성 출력이나 야외 GPS 이동을 실행 검증한 것은 아니다.

## 13. 미션 성공 기준 확정 — 12절의 미션 미확인 설명을 대체

사용자가 성공 기준을 **목적지 GPS 도착 + 예상 시간 + 거리**로 변경했고, 추가 질문에 **거리 ±5% · 시간 ±10%**를 명시적으로 선택했다.

- 출발 전 선택한 코스의 실제 경로 거리와 `거리 × 목표 페이스 + 예상 신호 대기`를 기준값으로 고정한다.
- 목적지 GPS 도착 판정(기존 순환 경로 진행 및 연속 도착 확인 포함)이 있고, 기록 거리 ±5%, 경과 시간 ±10%를 모두 만족하면 성공 멘트를 재생한다.
- 경계값을 포함한다. 너무 빠르거나 느려도 범위 밖이면 일반 도착 멘트다. 기준 데이터 누락/비정상 값도 일반 도착이다.
- **신호 조건 준수, 별도 페이스 범위, 추천 플래그, 안내 종료 여부는 이제 독립 성공 조건이 아니다.** 최신 사용자 지정 세 조건으로 판정한다.
- 직접 종료는 도착 미션 성공으로 처리하지 않는다.
- 결과의 `RunRecord.missionComplete`에 판정을 저장한다.
- `RunAnnouncementsChecks`에 하한/상한/경계 바로 밖/GPS 미도착/정보 누락/NaN 검사를 추가하여 통과했다.
- 빌드 성공: `/tmp/GrunnY-mission.log`. 기기 이동 실행 검증은 하지 않았다.

## 14. 해커톤 탐색 실패 대비 — ‘정확히 두 개·±5% 필수’ 조건 변경

사용자가 **결과 우선: 1개·대체 코스 허용**을 명시적으로 선택했다.

- 거리 ±5%인 유효 순환 코스를 우선하고, 부족하면 실제로 조회한 연결된 순환 코스 중 거리 차이가 적은 후보로 채운다. 최대 두 개, 하나만 찾으면 하나를 보여준다.
- 거리 보정 이전에 얻은 유효 코스도 버리지 않고 보관한다. 같은 경로의 동일 좌표 응답을 중복으로 넣지 않는다.
- 도로 연결성·순환 면적 검사는 유지한다. 가짜 직선이나 복제된 두 번째 코스를 만들어내지 않는다.
- 대체 코스가 있으면 기존 지도 아래 설명에 실제 거리 확인 안내를 표시한다. 기존 디자인은 유지한다.
- 최근 성공 결과를 메모리에 보관한다. 재탐색에서 결과가 전혀 없을 경우, 동일 목표 거리·출발점 10m 이내·15분 이내 결과만 복구한다. 다른 출발지의 코스를 가져오지 않는다. 앱 재실행 후 유지되는 디스크 경로 캐시는 아니다.
- 지도 서비스가 모든 보행 요청에 실패하고 이전 결과도 없으면 코스 제공은 보장할 수 없다. 사용자에게 이 한계를 설명했다.
- `RouteFallbackPolicy.swift` 및 `Tests/RouteFallbackChecks.swift` 추가: 한 후보, 거리 우선순위, 대체 후보, 잘못된 값, 시설 수 정렬 검사 통과.
- 빌드 로그 `/tmp/GrunnY-fallback.log`.
